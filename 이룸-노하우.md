# 이룸세무법인 홈페이지 제작 노하우

> 이 문서는 `설계서.md`(무엇이 만들어져 있는가)·`지침.md`(어떻게 재사용하는가)와 달리,
> **왜 이렇게 만들었고, 만드는 과정에서 무엇이 잘못됐다가 어떻게 고쳐졌는지**를 기록한 회고 문서입니다.
> 다른 업종(1차 대상: 에어컨 청소업체 "클린팩토리") 사이트를 새로 만들 때 시행착오를 반복하지 않기 위한 참고용입니다.
> 근거는 git 커밋 히스토리(`git log`, `git show`)와 실제 코드를 직접 대조해서 확인한 내용입니다.

## 0. 먼저 확인된 사실: 문서-코드 드리프트

`설계서.md`의 섹션 구성표(4장)에는 hero/services/profile/location/youtube/refund/kakao 7개만 있지만,
실제 `index.html`에는 그 사이에 **`#stats`(현황 통계)** 섹션과 **`#news`(최신소식)** 섹션이 추가되어 총 9개 섹션입니다.
둘 다 초기 설계 이후 나중에 추가된 섹션인데(`94a62a7`, `b2d7de1` 커밋), 이후 설계서 갱신 시 섹션 표를 함께 고치지 않아 문서가 실제 코드보다 뒤처졌습니다.

**교훈**: 섹션을 추가/삭제할 때마다 `설계서.md`의 섹션 구성표(4장)를 그 자리에서 함께 갱신한다. "나중에 한꺼번에 문서화"는 실제로 안 지켜짐.

---

## 1. 섹션 구성과 순서 — 판단 근거

실제 순서(위→아래): **헤더(고정) → hero → stats(현황) → services(주요업무) → profile(세무사) → location(지점) → youtube → refund(환급조회) → news(최신소식) → kakao(상담) → footer**

퍼널 관점의 판단 근거:

| 순서 | 섹션 | 배치 이유 |
|---|---|---|
| hero | 첫인상/약속 | 브랜드 슬로건 + 대표 경력을 영상 배경으로 임팩트 있게. 이 시점엔 아직 설득하지 않고 "무게감"만 전달 |
| stats | 숫자 신뢰 | 글로 설득하기 전에 재계약률 98%, 누적 상담 3만+ 같은 정량 지표로 먼저 신뢰를 심음(경쟁사 벤치마킹 후 추가, `327f75b`). 카운트업 애니메이션으로 체류 유도 |
| services | 핵심 서비스 | 검색 유입 사용자는 "양도세/세무조사/경정청구" 같은 구체적 니즈로 들어오므로, 사람(profile)보다 **먼저** "우리가 이 문제를 다룬다"를 보여줘야 이탈을 막음 |
| profile | 사람 신뢰 | "무엇을 하는지"에 공감한 다음에야 "누가 하는지"가 의미 있음 → services 다음 배치 |
| location | 행동 준비 | 신뢰가 쌓인 후 "그럼 어디로 가면 되나"라는 실용 정보 |
| youtube | 보조 콘텐츠 | 필수는 아니지만 전문성을 부담 없이 재확인시키는 콘텐츠. 전환 압박이 낮은 자리(중간~후반)에 배치 |
| refund | 소프트 전환 | 직접 상담 신청보다 심리적 장벽이 낮은 "환급금 조회"를 먼저 걸어 리드를 만드는 중간 CTA |
| news | 콘텐츠 마케팅 | 블로그 글 노출 — SEO/체류시간 보조 목적으로 나중에 추가된 섹션이라 순서상 뒤쪽에 위치 |
| kakao | 최종 전환 | 가장 직접적인 상담 행동. 신뢰를 다 쌓은 마지막에 배치 |

**배경색 교차 규칙**: navy 계열과 white/light-gray 계열을 인접 섹션끼리 번갈아 배치(연속 2개 이상 같은 계열 금지). 이렇게 하면 구분선 없이도 스크롤 리듬과 섹션 경계가 시각적으로 명확해짐. 이 규칙은 `지침.md` 4장에 이미 원칙으로 박제되어 있음.

**클린팩토리 관점에서 그대로 쓰이는 것**: "증거(숫자/사진) → 핵심 서비스 → 신뢰 요소 → 위치 → 콘텐츠 → 소프트 전환 → 직접 전환"이라는 퍼널 순서 자체는 업종 불문 재사용 가능한 뼈대입니다.

**클린팩토리 관점에서 달라져야 하는 것**:
- `profile`(세무사 개인 자격증 신뢰) 자리는 청소업에는 안 맞음 → 카드 마크업/hover-overlay/모바일-모달 구조는 그대로 재사용하되, 내용물을 "시공 전후 사진 갤러리" 또는 "장비/세제 소개"로 교체하는 편이 신뢰 형성에 더 적합.
- `refund`(세무 전용 기능) 자리는 재사용 불가 → 컴포넌트 골격(좌측 텍스트 + 우측 원형 배지, reveal-left/right)은 그대로 두고 내용을 "실시간 견적 계산기" 또는 "가격표/패키지 비교"로 교체.
- `location`은 **구조 자체가 근본적으로 달라짐** (3번 항목 참고).

---

## 2. 반응형 분기점과 각 단계 변화

고정 3단계(`1024px`, `899px`, `640px`)는 `지침.md`에 문서화된 그대로지만, 실제 CSS를 읽어보면 **경계가 하나 더** 있습니다: `min-width: 641px` "데스크톱 시인성 강화" 블록. 이건 처음 설계에 없던 것으로, 기본 폭에서 만든 스타일이 데스크톱에서 너무 작아 보인다는 피드백 이후 **나중에 덧붙인 패치**입니다(주석에 "Desktop 시인성 강화(모바일은 기존 크기 유지)"라고 스스로 명시). 즉 640/641을 기준으로 위아래 양방향에서 오버라이드가 걸리는 구조가 되어버렸습니다.

| 분기점 | 무엇이 바뀌는가 |
|---|---|
| `min-width:641px`(후행 패치) | 버튼/nav/hero 타이틀·설명/섹션 제목/카드 텍스트 등 폰트 크기를 전반적으로 15~30% 확대, hero-title을 중앙정렬→좌측정렬+하단 보더라인 추가, hero-desc-career를 2열 grid로 |
| `max-width:1024px` | services/profile/video 그리드 3→2열, refund/kakao 좌우 배치→1열(비주얼이 `order:-1`로 위로 올라옴) |
| `max-width:899px` | location 3열→1열(지도가 `order:-1`로 맨 위), 점선 커넥터 숨김(CSS `display:none` + JS `matchMedia('(min-width:900px)')`로 **이중으로** 방지), news 4→2열 |
| `max-width:640px` | 헤더 햄버거 슬라이드 메뉴, hero 텍스트/통계 축소, services가 그리드→가로 캐러셀로 전환, 지점카드가 "전화 아이콘 버튼"으로(반대로 PC는 "전화번호 텍스트"로 — 서로 반전됨), profile이 hover-오버레이 카드 → 3열 압축 썸네일+모달로, video/news 1열 |

**교훈**: 처음부터 "베이스=모바일, 위로 갈수록 강화"든 "베이스=데스크톱, 아래로 갈수록 축소"든 **한 방향으로 일관된 모바일-퍼스트/데스크톱-퍼스트 체계**를 정하고 시작해야 한다. 이번 프로젝트처럼 나중에 "그냥 min-width 블록 하나 추가"로 땜질하면, 같은 640 경계에 대해 CSS 여러 군데(base, min-width:641, max-width:640)를 동시에 봐야 값을 추적할 수 있어 유지보수 비용이 커짐. 클린팩토리는 시작 단계에서 3단계 표를 먼저 확정하고 "각 크기에서 폰트/레이아웃 최종값"을 표로 적어둔 뒤 코딩하는 게 낫습니다.

---

## 3. reveal 애니메이션 구현 방식

**CSS**: `.reveal`(translateY(28px))/`.reveal-left`(translateX(-40px))/`.reveal-right`(translateX(40px)) 3종 모두 `opacity:0` 시작, `.is-visible`이 붙으면 원위치+opacity:1로, `transition: opacity 0.7s ease, transform 0.7s ease`.

**JS(`setupScrollReveal()`)**: 페이지의 모든 `.reveal*` 요소를 하나의 `IntersectionObserver`(`threshold:0.15`, `rootMargin:"0px 0px -60px 0px"`)로 관찰. 뷰포트에 걸치면 `is-visible` 클래스를 붙이고 **즉시 `unobserve`** — 1회성 애니메이션이며 스크롤을 되돌려도 다시 숨지 않음. `IntersectionObserver` 미지원 브라우저는 전부 즉시 `is-visible` 처리(콘텐츠가 영원히 숨는 사고 방지).

**그리드 stagger는 JS가 아니라 순수 CSS로 처리**: `services-grid`/`profile-grid`/`video-grid` 각각에 `nth-child` 기반 `transition-delay`(0s/0.08s/0.16s 등)를 걸어둠. Observer는 그냥 동시에 `is-visible`을 붙일 뿐이고, 순차 등장처럼 보이는 건 전적으로 CSS transition-delay 차이 덕분. → JS와 CSS 책임을 깔끔히 분리한 부분이라 재사용 가치가 높음.

**숫자 카운트업은 별도 시스템**: `setupHeroStats()`가 `heroStats` 컨테이너 하나만 별도 Observer(threshold 0.3)로 관찰하다가 트리거되면, 각 숫자에 `i*150+150ms`만큼 지연을 주고 `requestAnimationFrame` + cubic ease-out으로 0→목표값을 채움. reveal 시스템과 별개로 둔 이유: 이건 "클래스 토글"이 아니라 "숫자를 실제로 바꿔야" 하는 JS 애니메이션이라 범용 reveal 옵저버에 얹을 수 없었기 때문.

**함정(반드시 알아야 할 것)**: 모바일 캐러셀로 전환된 `services-carousel` 내부의 카드에는 reveal 애니메이션을 **강제로 무효화**(`opacity:1; transform:none; transition:none`)해야 합니다. 이유: 캐러셀이 카드를 옆에서 슬라이드로 밀어 넣을 때마다 그 카드가 처음 화면에 "보이는" 순간이 되어 개별 Observer가 뒤늦게 반응, translateY(상승)과 캐러셀의 translateX(좌우이동)이 겹쳐 "대각선으로 튀어오르는" 것처럼 보이는 버그가 있었습니다(`f9aa937`). **자동 슬라이드/캐러셀 요소와 scroll-reveal을 같은 요소에 동시 적용하지 말 것** — 컨테이너 단위로만 reveal을 걸고, 내부 아이템은 캐러셀 로직에 전담시켜야 함.

---

## 4. 겪은 UX 버그와 해결책

### 4-1. 모바일 뒤로가기가 오버레이를 안 닫고 페이지를 이탈시키는 문제 (가장 중요한 재사용 패턴)

**증상**: 햄버거 메뉴(`42de582`)와 프로필 상세 모달(`f0d6c38`) 둘 다에서 동일하게 발생. 모바일 브라우저의 물리적/제스처 뒤로가기를 누르면 열려있는 오버레이를 닫는 게 아니라 사이트 자체를 이탈해버림 — 사용자 입장에선 "뒤로가기 눌렀는데 사이트가 통째로 사라짐"으로 느껴지는 심각한 이탈 유발 버그.

**해결 패턴**(양쪽에 동일하게 적용, 재사용 가치 높음):
```js
// 열 때: 더미 히스토리 엔트리를 하나 쌓아둔다
function open() {
  /* ...열기 처리... */
  history.pushState({ myOverlay: true }, "");
  historyPushed = true;
}
// 닫을 때: popstate로 닫힌 게 아니면(=X버튼/배경클릭/Esc) 쌓아둔 엔트리를 정리
function close(fromPopState) {
  /* ...닫기 처리... */
  if (historyPushed) {
    historyPushed = false;
    if (!fromPopState) history.back();
  }
}
// 기기 뒤로가기: 페이지 이탈 대신 오버레이만 닫는다
window.addEventListener("popstate", function () {
  if (isOpen()) close(true);
});
```
`historyPushed` 플래그로 "이미 닫힌 오버레이를 또 back() 하는" 이중 되감기를 방지하는 게 핵심. 새 오버레이(예: 견적 모달, 필터 패널 등)를 추가할 때마다 이 패턴을 그대로 복붙하면 됨.

### 4-2. 헴버거 X 버튼이 메뉴 패널에 가려 안 보이던 문제 (`1cca788`)

`.nav-toggle`에 `position`/`z-index` 지정이 없어서, `position:fixed`인 `.main-nav` 드로어 패널(`z-index:150`)이 열렸을 때 버튼이 그 뒤로 묻혀버림. `position:relative; z-index:160`을 명시적으로 줘서 해결.

**교훈**: fixed 오버레이(드로어/모달)를 추가할 때마다, "그 오버레이가 열려도 계속 보이고 눌려야 하는 트리거/닫기 버튼"의 z-index를 반드시 오버레이보다 높게 명시적으로 지정해야 함. 이 프로젝트의 z-index 값(150/160/200/200/999)은 규칙 없이 그때그때 붙여진 값이라 다음에 오버레이를 하나 더 추가하면 또 같은 사고가 날 수 있음 — **클린팩토리에서는 시작할 때 z-index 스케일표(예: header 100, drawer 150, drawer-trigger 160, modal 900+)를 만들어두고 새 오버레이 추가 시 그 표부터 참고**하는 걸 권장.

### 4-3. 프로필 사진 크로스페이드가 두 번 갈아엎어진 이야기 (`3569405` → `ceb3fa0`)

1차 버전(초기): `@keyframes profileCrossfade`가 각 사진당 opacity:1 구간을 전체 주기의 26%만 줘서, 2장짜리 카드(50% 간격)에서 두 사진이 동시에 opacity:0이 되는 약 24% 구간이 생김 → 전환 중 배경(카드 자체 배경색)이 그대로 노출되는 깜빡임 버그.

2차 수정: `0%→1, 25%→0.5, 50%→0, 75%→0.5, 100%→1`의 대칭 곡선으로 바꿔 "지연 오프셋이 걸린 모든 사진의 opacity 합이 항상 일정하게 유지"되도록 수학적으로 보장 → 깜빡임(빈 화면)은 해결됐지만, 이번엔 두 사진이 동시에 반투명 상태로 겹쳐 보이는 구간이 "심령사진 같다"는 피드백.

3차(최종) 수정: 겹침 자체를 없애고 **하드컷**으로 변경 — `0%→1, 49.9%→1, 50%→0, 99.9%→0, 100%→1`. 즉 사진이 절반 주기 동안 완전 불투명으로 유지되다가 즉시 다음 사진으로 전환. N장(2장/4장)에 대해 delay를 `duration/N` 간격으로 정확히 배분하면, 언제나 정확히 한 장만 보이는 크로스페이드가 됨.

**교훈**: 여러 이미지를 독립적인 `animation-delay`만으로 크로스페이드 시키는 순수 CSS 루프는, "매 순간 정확히 몇 장이 보이는가"를 N장에 대해 미리 손으로 검산해야 함. 눈대중으로 키프레임을 짜면 겹침(심령사진)이나 빈틈(깜빡임) 둘 중 하나가 반드시 생김. **최종적으로 검증된 공식(하드컷, delay=duration/N)을 다음 프로젝트에서 처음부터 그대로 재사용**하면 두 번 갈아엎을 필요가 없음.

### 4-4. PC/모바일 인터랙션 패턴이 여러 번 뒤집힌 프로필 UX (`fc7f663` → `bd420ba` → `d0f304e`/`e5b5a6c`)

처음엔 PC도 "클릭 시 모달"로 통일하려 했으나, "PC에서 클릭 한 번 더 요구하는 게 비효율적"이라는 피드백으로 PC는 원래의 hover-오버레이 방식으로 되돌림. 모바일은 hover 자체가 안 통하는 데다 카드 위 오버레이가 좁아 가독성이 떨어져, 결국 사진(3:4)+경력 텍스트를 담은 별도 상세 모달로 정착. 그 사이 "모달에 사진을 뺐다가 다시 넣는" 변경도 있었음.

**교훈**: hover는 터치 기기에서 애초에 작동하지 않으므로 "하나의 인터랙션이 모든 디바이스에 통한다"고 가정하지 말 것. **처음부터 "PC=hover, 모바일(≤640px)=클릭→상세모달"로 분기 설계**하고 시작하면, 이번처럼 여러 차례 갈아엎는 커밋 없이 한 번에 정착시킬 수 있음.

### 4-5. 지도 커넥터 라인의 좌표 계산 타이밍

`setupLocationConnectors()`는 `getBoundingClientRect()`로 핀↔카드 위치를 런타임 계산하는데, 초기 `draw()` 한 번뿐 아니라 `window.addEventListener("load", draw)`를 별도로 걸어둠. 지도 이미지가 완전히 로드되기 전에 계산하면 레이아웃이 아직 확정 안 된 상태의 좌표로 선을 그려 어긋날 수 있기 때문. 리사이즈는 150ms 디바운스로 재계산.

**교훈**: 이미지가 포함된 레이아웃 위에서 좌표 기반 오버레이(선, 핀 등)를 그릴 때는 `DOMContentLoaded` 시점이 아니라 **이미지까지 로드가 끝난 `load` 시점에 한 번 더 재계산**해야 안전함.

---

## 5. SEO — 나중에 뒤늦게 추가해야 했던 것들

실제 배포 이후 시간이 꽤 지나서야 하나씩 추가된 항목들입니다(커밋 시간순):

1. **robots.txt / sitemap.xml 자체가 초기 배포엔 없었음** — 사이트를 검색엔진에 정식 등록하려는 시점(`a0592ec`)에야 처음 생성. 그 전까지는 크롤러가 사이트맵 위치를 알 방법이 없는 상태로 방치됨.
2. **canonical/OG/JSON-LD URL의 www 불일치** — 서버는 non-www→www로 308 리다이렉트하고 있었는데, 메타태그들은 non-www를 가리키고 있어 검색엔진에 혼란 신호를 주고 있었음. 실제 서비스 도메인과 메타태그 URL을 나중에 한 번에 맞추는 정리 작업이 필요했음.
3. **네이버 서치어드바이저 소유확인 파일** — 구글/일반 SEO 체크리스트에는 없는, 네이버 등록 시점에야 알게 되는 한국 시장 특화 요구사항. 발급된 `navera{...}.html`은 사이트 루트에 영구 보관해야 함(삭제 시 인증 해제).
4. **공유 미리보기(OG) 이미지 크롭** — 처음엔 헤더용 와이드 배너 로고를 그대로 `og:image`에 썼다가, 실제로 카카오톡에 링크를 공유해보고 나서야 정사각형 크롭으로 로고 텍스트가 잘리는 걸 발견(`73d6712`). 정사각형 전용 `og-logo.png`를 별도 제작해서야 해결.
5. **파비콘** — 초기 배포엔 브랜드 로고 기반 파비콘이 없었고, 한참 뒤에야(`87d947d`) 실제 로고 기반 favicon.ico/16x16/32x32/apple-touch-icon 세트로 교체.
6. **meta description 길이** — 일반적인 SEO 감각으로 작성한 설명 문구가 네이버의 권장 길이(80자 이내)보다 길어서, 나중에 별도로 축약하는 커밋(`50cc12c`)이 필요했음. 구글 기준(~155자)과 네이버 기준이 다르다는 걸 뒤늦게 알게 된 경우.

**교훈 및 클린팩토리 적용**: 이 항목들은 이미 `지침.md` 13~14장에 "새 프로젝트 착수 시 처음부터 할 일"로 정리되어 있습니다. 즉 이번 프로젝트가 몸으로 겪은 시행착오가 지침으로 코드화되어 있는 상태이므로, **클린팩토리는 이 항목들을 "배포 후 정리"가 아니라 초기 배포 체크리스트에 처음부터 포함**시키면 됩니다. 특히 지역별 페이지를 여러 개 만들 계획이라면, 각 지역 페이지마다 canonical/OG/sitemap 등록을 빠짐없이 해야 하므로 이 체크리스트의 중요도가 이룸 때보다 더 커집니다(6장 참고).

---

## 6. 다시 만든다면 처음부터 다르게 했을 점

1. **데이터-마크업 분리를 맨 처음부터**: `지침.md`가 이미 "TEAM/BRANCHES/VIDEOS는 JS 데이터로 분리돼야 하는데 현재는 HTML에 직접 기술돼있다"고 자인하고 있음. 실제로 인물 정보 수정 커밋 다수(`9bd1a92`, `cea533f`, `a9fa67c`, `1577787` 등)가 반복되는 대형 HTML 블록을 손으로 찾아 고치는 방식이었고, 그 과정에서 오탈자(`1577787`: "동래용인세무서장"→"동래세무서장", "전경훈"→"정경훈")도 실제로 발생했습니다. 처음부터 데이터 배열 + 템플릿 렌더링으로 시작했다면 이런 종류의 복붙 오타 자체가 구조적으로 줄어들었을 것.
2. **반응형 체계를 한 방향으로 확정**: `min-width:641px` 패치 블록을 나중에 덧붙이는 대신, 모바일/데스크톱 중 하나를 베이스로 정하고 처음부터 일관된 방향으로 오버라이드를 쌓았어야 함(2장 참고).
3. **PC/모바일 인터랙션을 처음부터 분기 설계**: hover(PC)와 클릭-모달(모바일)을 처음부터 나눠 설계했다면 프로필 UX를 세 번 갈아엎지 않아도 됐음(4-4 참고).
4. **크로스페이드 애니메이션은 검증된 공식을 바로 사용**: "하드컷 + delay=duration/N" 공식을 처음부터 알고 시작하면 됨(4-3 참고).
5. **SEO 체크리스트를 초기 배포 단계에 포함**: 5장의 6개 항목 전부 "배포 후 뒤늦게" 대신 "배포 전 체크리스트"로 앞당길 것.
6. **오버레이 z-index를 표로 관리**: 새 오버레이를 추가할 때마다 사고가 나지 않도록, z-index 스케일을 문서 한 곳에 정해두고 시작(4-2 참고).
7. **설계서.md를 살아있는 문서로 유지**: 섹션을 추가/삭제하는 커밋에는 반드시 `설계서.md` 섹션 표 갱신을 같은 커밋(또는 바로 다음 커밋)에 포함시킬 것 — 이번처럼 stats/news가 반영 안 된 채 방치되는 일을 방지.

---

## 7. 클린팩토리(에어컨 청소업, 지역별 페이지 분리) 관점 정리

### 그대로 쓸 수 있는 것
- 전체 퍼널 순서(증거→서비스→신뢰→위치→콘텐츠→소프트전환→전환), navy/white 배경 교차 규칙
- CSS 변수 기반 컬러 시스템, `:root`만 바꾸는 커스터마이징 방식
- reveal 3종 애니메이션 시스템(CSS 클래스 + 단일 IntersectionObserver + nth-child stagger)
- 모바일 무한 캐러셀 컴포넌트(서비스/패키지 카드가 많을 때)
- **모바일 뒤로가기 = 오버레이 닫기 패턴**(4-1) — 새로 만들 견적 모달/필터 패널 등에도 그대로 적용
- 헤더 로고 흰색 실루엣 처리(`filter: brightness(0) invert(1)`), 스크롤 시 반투명 네이비 배경
- 전화 클릭-투-콜 버튼(모바일 아이콘/PC 텍스트 반전 패턴)
- 카카오 채널/QR 코드 fallback 패턴, 유튜브 모달, GitHub+Vercel 무DB 배포 워크플로우
- SEO 체크리스트(robots.txt/sitemap.xml/네이버 소유확인/OG 정사각 이미지/파비콘/메타디스크립션 80자)

### 달라져야 하는 것 (지역별 페이지 분리가 핵심 변수)
- **location 섹션의 구조 자체가 바뀜**: 이룸은 "전국 지도 한 장 위에 10개 지점 핀 + 확대 인셋"을 **하나의 원페이지 안에서** 보여주는 방식이었지만, 클린팩토리는 지역별로 **별도 페이지**(예: `/suwon`, `/busan` 등)를 만들 계획이므로,
  - 전국 지도+핀+커넥터라인 시스템은 "지역 허브(전체 서비스 지역 찾기)" 역할을 하는 메인 페이지 한 곳에만 남기고,
  - 개별 지역 페이지 안에는 그 지역 하나만 다루는 단순 주소/네이버지도 임베드 정도로 축소하는 게 합리적.
- **데이터-마크업 분리가 "권장"에서 "필수"로 바뀜**: 지역 페이지가 여러 개면 헤더/푸터/CSS/공통 섹션 구조를 페이지마다 복붙할 수 없음. 공통 템플릿 + 지역별 데이터(지역명/주소/전화/사진/후기/요금)를 분리하고, 정적 사이트 빌드 스크립트(템플릿+JSON→지역별 html 생성)를 미리 만들어두는 게 사실상 필수. 이번 프로젝트가 "언젠가 리팩터링하면 좋다" 수준으로 미뤄뒀던 부분이 클린팩토리에서는 시작부터 있어야 함.
- **지역별 SEO 메타데이터**: 지역 페이지마다 canonical/title/description/JSON-LD를 지역명 키워드로 각각 다르게 채워야 함(콘텐츠가 서로 비슷하면 중복 콘텐츠로 인식되어 개별 지역 키워드 노출에 불리). sitemap.xml에도 지역 페이지 URL을 각각 등록.
- **profile 섹션의 내용 성격 교체**: "세무사 개인 자격증" 신뢰 요소 대신 "시공 전후 사진/장비/보험가입 여부" 등으로 콘텐츠만 교체(컴포넌트 구조는 재사용).
- **refund 섹션 자리 교체**: 세무 전용 "환급조회"를 "실시간 견적 계산기" 또는 "가격표/패키지 비교"로 교체(컴포넌트 골격은 재사용).
- **stats 섹션의 숫자 재정의**: "재계약률/절세 누적액" 대신 "시공 건수/평균 만족도/보유 장비 수/서비스 지역 수" 등 업종에 맞는 지표로 교체.
