# 홈페이지 템플릿 활용 지침

> 이 문서는 현재 프로젝트(이룸세무법인 홈페이지)를 **다른 업체(제2, 제3 사이트)용 템플릿**으로 재사용할 때 지켜야 할 규칙과 절차를 정리한 가이드입니다.
> "무엇이 만들어져 있는지"는 `설계서.md`를 참고하세요.

## 1. 기본 원칙

1. **구조 고정, 콘텐츠만 교체**: 섹션 순서(hero → services → profile → location → youtube → refund → kakao), navy/white(계열) 배경 교차 패턴, reveal 애니메이션 방향은 새 프로젝트에서도 그대로 유지합니다. 바꾸는 것은 색상 변수·문구·이미지·데이터입니다.
2. **CSS 변수만 수정, 하드코딩 색상 금지**: 새 프로젝트에서 브랜드 컬러가 다르면 `:root`의 변수 값만 바꿉니다. CSS 본문에 `#0F2A4A` 같은 색상을 직접 새로 넣지 않습니다.
3. **데이터-마크업 분리 원칙(현재 미적용 상태 — 개선 필요)**: 설계 의도상 `TEAM`/`BRANCHES`/`VIDEOS`는 JS 상수로 분리되어야 하나, 현재는 `index.html`에 직접 기술되어 있습니다. 새 프로젝트를 만들 때는 **가능하면 이 부분부터 JS 데이터 배열 + 템플릿 렌더링 방식으로 분리**해서 시작하는 것을 권장합니다. 그래야 인물/지점 수가 다른 새 업체에도 빠르게 대응할 수 있습니다.
4. **고정 3단계 브레이크포인트 유지**: `1024px`(3→2열), `899px`(지점 3열→1열, 점선 숨김), `640px`(모바일 nav, 전체 1열). 새 CSS를 추가할 때도 이 3개 기준만 사용하고 임의의 브레이크포인트를 늘리지 않습니다.

## 2. 새 프로젝트 시작 체크리스트

1. 프로젝트 폴더 복제 (`index.html`, `assets/css/style.css`, `assets/js/main.js`, `assets/images/`, `assets/video/`)
2. 폴더/파일명은 **영문+하이픈**으로 (한글+공백 폴더명은 배포 환경에서 경로 인코딩 문제 발생 가능)
3. `:root` CSS 변수 값 교체 (아래 3항 참고)
4. `<head>` 메타정보 전체 교체: title, description, keywords, canonical URL, OG/Twitter 태그, Schema.org JSON-LD(`AccountingService` → 업종에 맞는 타입으로), favicon, logo
5. 폰트가 그대로 필요 없다면 `--font`/`--font-en` 및 `<link>` 폰트 로딩 교체
6. 섹션별 텍스트/이미지/데이터 교체 (아래 5항 참고)
7. 지도/핀 좌표 재계산 (아래 8항 참고) — 지점 안내가 필요 없는 업종이면 location 섹션 자체를 통째로 제거
8. `main.js`의 `SITE_CONFIG` 값(카카오 채널 URL 등) 업데이트
9. 정적 검증: `node -c assets/js/main.js` 문법 체크, 이미지 경로 존재 여부 확인
10. 실제 브라우저 렌더링 확인 (반응형 3단계 + 애니메이션 동작)

## 3. 컬러/폰트 커스터마이징

`assets/css/style.css` 최상단 `:root` 블록의 아래 변수만 바꾸면 전체 배색이 따라 바뀝니다.

| 변수 | 역할 | 바꿀 때 주의점 |
|---|---|---|
| `--navy`, `--navy-dark`, `--navy-soft` | 메인 브랜드색 3단계(밝음→어두움) | 명도 차이를 유지해야 그라데이션이 자연스러움 |
| `--gold`, `--gold-light` | 포인트/강조색 | 버튼, hover, 통계 숫자 등에 전역 사용되므로 대비가 잘 보이는 색으로 |
| `--kakao`, `--kakao-text` | 카카오 버튼 전용 | 카카오 채널을 안 쓰면 해당 버튼 자체를 다른 CTA로 교체 |
| `--ink`, `--gray-*` | 텍스트/보조 배경 | 보통 유지, 브랜드 톤이 명확히 다를 때만 미세 조정 |
| `--font`, `--font-en` | 한글/영문 폰트 | 폰트를 바꾸면 `<head>`의 CDN `<link>`도 함께 교체 |

색상 교체 후 반드시 확인할 곳: hero 배경 그라데이션(`heroGradient` keyframes), location/refund 섹션 navy 그라데이션, 버튼 hover 색상.

## 4. 섹션 추가/제거 규칙

- **배경 교차 유지**: 섹션을 추가/삭제해도 인접 섹션끼리 navy와 white가 번갈아 오도록 배치합니다. (연속으로 같은 계열 배경 2개를 붙이지 않음)
- **reveal 방향 규칙**:
  - 단일 컬럼 콘텐츠(위→아래 스택) → `.reveal` (하단에서 슬라이드업)
  - 좌측 컬럼 콘텐츠 → `.reveal-left`
  - 우측 컬럼 콘텐츠 → `.reveal-right`
  - 카드형 그리드(3열 이상 반복 요소) → `.reveal` + `nth-child` 기반 stagger `transition-delay`
- 새 섹션의 JS 동작이 필요하면 `main.js`에 `setupXxx()` 함수를 새로 만들고, `init()` 호출 순서 규칙(8항)에 맞는 위치에 호출부를 추가합니다(기존 12개 함수와 동일한 네이밍/호출 패턴 유지).

## 5. 데이터 교체 가이드 (팀/지점/영상)

### 세무사(팀원) 정보
- `.profile-card` 반복 블록을 인원 수만큼 복제
- 사진은 `assets/images/profiles/{이름-로마자}-{순번}.jpg` 네이밍 규칙 유지, 인물당 2~4장
- 사진이 여러 장이면 `.photo-fade` + `profileCrossfade` keyframe이 자동으로 크로스페이드 처리(추가 JS 불필요)
- 사진이 1장뿐이면 크로스페이드 관련 마크업(두 번째 `<img>`) 생략 가능

### 지점 정보
- `.branch-card`를 좌/우 컬럼에 나눠 배치 (지역 그룹 기준으로 좌우 균형을 맞출 것)
- 각 카드의 `data-pin` 값은 지도 위 `.map-pin`의 `data-pin` 값과 반드시 1:1로 일치해야 점선 연결이 동작
- `data-naver-url`에 실제 네이버 지도 검색/공유 링크 입력

### 영상 정보
- `.video-card`에 `data-video-id`(유튜브 영상 ID)만 넣으면 썸네일은 `i.ytimg.com`에서 자동 로드, 클릭 시 `setupVideoModal()`이 iframe을 지연 생성

## 6. 이미지 에셋 규칙

```
favicon.ico / favicon-16x16.png / favicon-32x32.png / apple-touch-icon.png  (루트에 위치, 로고 원본에서 생성)
assets/images/
  logo.png
  hero-poster.jpg            (히어로 영상 첫 프레임, 영상 로딩 전 표시용 — 필수 아님)
  location/korea-map.png     (해당 지역 지도로 교체)
  profiles/{이름-로마자}-{순번}.jpg
  kakao/qr-code.png          (없으면 .qr-fallback 자동 표시, 파일만 넣으면 자동 대체)
assets/video/{파일명}.mp4/.webm  (없으면 CSS 그라데이션으로 자동 대체 — 필수 아님, 파일명은 자유롭게 지정 가능하고 index.html의 <source> 경로만 맞추면 됨)
```
- 파일이 없을 때 자동 폴백되는 자산(hero 영상, 카카오 QR)은 **없어도 사이트가 깨지지 않도록 설계된 부분**이므로, 새 프로젝트에서 준비가 늦어도 먼저 배포 가능
- 지도 이미지가 없는 업종(전국 단일 사무실 등)은 location 섹션 전체를 제거하거나 단순 주소 카드형으로 축소

## 7. 지도/핀 좌표 커스터마이징 절차

다른 지역 지도로 교체하거나 기존 핀 좌표를 실제 위치에 맞게 보정할 때 사용하는 절차 (퍼센트 기반 좌표 방식):

1. 새 지도 이미지를 `assets/images/location/korea-map.png` 자리에 교체
2. `.map-stage`는 `position:relative; width:100%`이고 내부 `<img>`는 `width:100%; height:auto`이므로, 핀의 `left/top %`는 **이미지 원본 픽셀 가로/세로 비율과 그대로 1:1 대응**됨 (컨테이너에 별도 높이 고정이 없기 때문)
3. **주소 기반 좌표 보정(육안 어림보다 정확)**:
   - 각 지점 주소(도로명주소)를 대략의 위도/경도로 환산 (예: 시/군/구 단위 대표 좌표만으로도 충분 — 아이콘 지도는 정밀 지도가 아니므로 상세 지번 단위 정밀도는 불필요)
   - 지도 이미지에서 실제 육지 모양이 시작/끝나는 픽셀 경계(최북단·최남단·최서단·최동단)를 `ffprobe`로 이미지 크기 확인 후 확대 크롭(ffmpeg `crop`+`scale`+`drawgrid`)해서 육안으로 특정
   - 위 두 값으로 선형 보간 공식을 세움: `x% = (경도-서쪽경계)/(동쪽경계-서쪽경계) * (오른쪽픽셀%-왼쪽픽셀%) + 왼쪽픽셀%`, `y%`도 위도 기준으로 동일하게(북쪽이 0%에 가깝도록 부호 반전) 계산
   - 계산된 좌표를 바로 코드에 넣지 말고, ffmpeg `drawbox` 필터로 지도 이미지 위에 임시 테스트 마커를 찍어 렌더링한 뒤 육지 안에 정확히 들어오는지, 지점 간 상대 위치(남북/동서 관계)가 실제 지리와 맞는지 시각적으로 검증 → 어긋나면 좌표를 미세 조정 후 재검증
   - 검증이 끝난 좌표만 `.map-pin`의 `style="left:xx.x%;top:yy.y%"`에 반영
4. `.map-pin` 버튼 각각에 좌표 인라인 스타일과 `data-pin`, `aria-label`, `data-naver-url` 설정
5. 좌/우 지점 카드 수가 5:4처럼 균형 맞게 배분되도록 지역별로 묶어서 좌우 컬럼에 배치
6. `setupLocationConnectors()`는 좌표를 하드코딩하지 않고 `getBoundingClientRect()`로 핀-카드 위치를 런타임 계산하므로, 좌표만 바꾸면 점선 연결선은 자동으로 재계산됨(900px 이상에서만 표시)

### 밀집 지점 확대 인셋(줌 버블)

여러 지점이 지도상 좁은 영역에 몰려 핀이 겹쳐 보일 때, 해당 영역만 원형으로 확대해 보여주는 방식:

1. ffmpeg `crop`+`scale`(lanczos)로 원본 지도에서 밀집 영역만 잘라 확대한 별도 이미지 생성 (예: `assets/images/location/korea-map-zoom-{지역명}.png`)
2. `.map-stage` 내부에 `.map-zoom-inset`(원형, `border-radius:50%; overflow:hidden`, `aspect-ratio:1/1`, `width:N%`) 컨테이너를 클러스터 중심 좌표에 배치하고, 그 안에 확대 이미지(`.zoom-inset-img`)를 넣음
3. 해당 클러스터의 `.map-pin` 버튼들은 `.map-stage`가 아닌 `.map-zoom-inset` 내부로 옮기고, 좌표는 크롭 영역 기준 로컬 퍼센트로 재계산 (`(원본픽셀-크롭원점)/크롭크기*100`). 인셋이 좁아 핀끼리 겹치기 쉬우므로 각 핀 간 로컬% 거리가 충분히(원의 반지름 대비 여유 있게) 떨어지도록 배치
4. `data-pin`/`aria-label`/`data-naver-url`은 그대로 유지 — `setupLocationConnectors()`는 DOM 위치 기준으로 동작하므로 핀이 인셋 내부로 이동해도 별도 JS 수정 불필요

## 8. 코딩 컨벤션

- **클래스 네이밍**: BEM 유사 패턴 `.block`, `.block-part`, `.is-state`(`is-visible`, `is-active`, `is-scrolled`) — 새 클래스도 이 규칙을 따름
- **JS 함수 패턴**: 모든 초기화 로직은 `setupXxx()` 형태의 독립 함수로 작성하고, 부작용 없이 `init()`에서만 호출. 함수 간 직접 호출 대신 각자 필요한 DOM을 자체적으로 `querySelector`
- **`init()` 호출 순서 규칙**: 레이아웃 관련(헤더/네비) → 애니메이션 관찰자 → 콘텐츠 인터랙션(프로필/지도/영상) → 기타(카카오, 히어로 폴백) 순서를 유지. 새 setup 함수는 성격에 맞는 위치에 삽입
- **애니메이션 옵션 통일**: `IntersectionObserver` 사용 시 기존과 동일하게 `threshold: 0.15`, `rootMargin: "0px 0px -60px 0px"`, transition `0.7s ease` 값을 재사용해 일관된 속도감 유지
- **접근성 필수 요소**: 모든 인터랙티브 요소(핀, 카드, 버튼)에 `tabindex`/키보드(Enter, Space) 대응 및 `aria-label` 부여, `skip-link` 유지

## 9. 헤더 로고/스크롤 색상 처리 패턴 (재사용 기법)

컬러 로고를 어두운 히어로 배경 위에 올리는 경우 흔히 겪는 문제: 로고 이미지 내 일부 텍스트가 스크롤 시 배경과 겹쳐 안 보이는 현상. 해결 패턴은 다음과 같습니다.

```css
.logo img { filter: brightness(0) invert(1); }  /* 로고를 항상 흰색 실루엣으로 */
```

- 헤더 배경이 투명(히어로 위)이든 스크롤 후 반투명 네이비든, **로고는 스크롤 상태와 무관하게 항상 동일한 필터를 유지**시키는 것이 가장 안전합니다.
- "스크롤 시 원래 컬러 로고를 보여주고 싶다"는 요구가 있다면, 그 경우에만 헤더 스크롤 배경을 **흰색 계열**로 바꿔서 원색 로고와 대비가 되도록 설계해야 합니다(네이비 배경 + 컬러 로고 조합은 항상 흰색 필터 고정 권장).
- 로고/메뉴 글자 크기는 데스크톱과 모바일(640px 이하)을 분리해서 지정: 데스크톱은 가독성 위주로 여유 있게, 모바일은 슬라이드 메뉴 공간에 맞게 별도 축소값을 명시적으로 오버라이드.

## 10. 히어로 배경 영상 제작 기법 (무료 스톡 영상 조합)

업체가 자체 촬영 영상이 없는 경우, 무료 스톡 영상 여러 개를 짧게 잘라 크로스페이드로 이어붙여 하나의 배경 루프 영상을 만드는 방식을 사용합니다.

1. Pexels/Pixabay/Coverr/Mixkit 등에서 업종에 맞는 클립 3~5개 다운로드 (검색 키워드는 업종 특성 반영: 예 세무/회계 → "calculator", "office desk", "city skyline")
2. `ffmpeg`로 각 클립을 동일 해상도(1920x1080)·동일 fps·동일 픽셀포맷으로 정규화 후 4~5초씩 트리밍
3. `xfade` 필터로 클립 사이 0.5~0.8초 디졸브 전환을 체인으로 연결 (오프셋 = 누적 길이 - 전환 길이)
4. 전체 결과물 시작/끝에 페이드 인·아웃(0.5~0.6초)을 추가해 루프 재생 시 이음새가 어색하지 않도록 처리
5. 오디오 트랙 제거(`-an`, 어차피 `muted` 속성), `-c:v libx264 -crf 26~28 -movflags +faststart`로 압축해 5~10MB 내외로 유지
6. 첫 프레임을 `-frames:v 1`로 추출해 `hero-poster.jpg`로 저장하고 `<video poster="...">`에 지정 (영상 로딩 전 깜빡임/검은 화면 방지)
7. 총 재생 길이는 **15~20초 내외**가 적당 — 너무 짧으면 반복이 티나고, 너무 길면 파일 용량과 로딩이 부담됨

## 11. 배포 환경 구성 (GitHub + Vercel, DB/관리자 페이지 없는 구조)

콘텐츠 수정이 드물고(연 수회 이하) 실제 편집을 개발 도구(Claude Code 등)로 직접 진행하는 워크플로우라면, 별도 DB·백엔드·관리자 페이지 없이 아래 구조를 기본값으로 채택합니다.

1. 로컬 프로젝트 폴더에서 `git init` → `.gitignore`로 원본 소스 파일(원본 이미지, 압축파일, 로컬 설정 폴더 등) 제외 → `git add` / `git commit`
2. GitHub에 빈 저장소 생성 후 `git remote add origin ...` → `git push -u origin main`
3. Vercel 가입은 반드시 "Continue with GitHub"로 (별도 이메일 가입 시 저장소 연결이 번거로워짐), 저장소 import 시 Framework Preset은 정적 사이트 기준 **Other** 선택
4. 이후 워크플로우: 로컬 수정 → `git add/commit/push` → Vercel이 push를 감지해 자동 재배포 (자동 파일 감시가 아니라 push가 트리거이므로, 수정 후 반드시 push까지 완료해야 실제 사이트에 반영됨)
5. 정식 도메인은 별도로 구매한 뒤 DNS/네임서버만 배포 플랫폼으로 연결하면 되므로, **개발 초기 단계부터 GitHub+Vercel을 먼저 세팅**하고 정식 도메인 연결은 공개 시점에 맞춰 나중에 진행해도 무방
6. 비개발 직원의 잦은 교체·인수인계 리스크가 있는 조직일수록, 커스텀 DB+관리자 패널보다 이 표준 Git 워크플로우가 유지보수 부담이 적음 (범용 도구라 후임자·다른 개발자가 바로 이해 가능)

## 12. 모바일 무한 캐러셀 재사용 기법 (카드형 콘텐츠 자동 슬라이드)

카드가 4개 이상으로 많아 모바일에서 세로로 길게 스택되면 스크롤 피로도가 높아지는 경우, `#services` 섹션에 적용한 아래 패턴을 재사용할 수 있습니다 (목차형 서비스 카드, 팀원 카드 등 반복 요소 전반에 적용 가능).

1. **마크업**: 카드 그리드(`.xxx-grid`)를 `.xxx-carousel`(`position:relative; overflow:hidden; touch-action:pan-y;`)로 한 번 더 감싸고, 그리드 아래에 빈 `<div class="xxx-dots" id="xxxDots" aria-hidden="true"></div>`를 둡니다. 데스크톱에서는 `.xxx-dots { display:none; }`로 숨겨두고, 모바일 미디어쿼리 안에서만 `display:flex`로 노출합니다. `touch-action:pan-y`는 가로 스와이프 제스처와 세로 페이지 스크롤이 서로 방해하지 않도록 해줍니다.
2. **모바일 전용 CSS**: `max-width: 640px` 블록 안에서만 그리드를 `display:flex; flex-wrap:nowrap; transition:transform .6s ease;`로 바꾸고 각 카드를 `flex:0 0 100%`로 지정합니다(데스크톱 그리드 레이아웃은 그대로 둡니다).
3. **JS(`setupXxxCarousel()`)** — 양방향 무한 루프 + 터치 스와이프:
   - 첫 카드는 트랙 맨 끝에, 마지막 카드는 트랙 맨 앞에 각각 `cloneNode(true)`로 추가해 앞/뒤 양쪽에 "가짜 슬라이드"를 둡니다. 실제 카드 인덱스는 1부터 시작(`trackIndex`), 0은 앞쪽 클론, `total+1`은 뒤쪽 클론 위치입니다.
   - `setInterval`로 일정 간격(2.5~4초)마다 `trackIndex`를 증가시키며 `translateX(-trackIndex*100%)` 적용
   - `trackIndex`가 뒤쪽 클론 위치(`total+1`)에 도달하면 `transitionend` 이후 `transition:none` + 강제 리플로우(`void track.offsetWidth`)로 `trackIndex`를 1로 순간 복귀 → 계속 좌측으로만 흐르는 것처럼 보임. 반대로 앞쪽 클론 위치(0)에 도달하면 동일한 방식으로 `total`로 복귀시켜 우측 스와이프(이전 슬라이드) 방향도 끊김 없이 무한 반복되게 합니다.
   - **터치 스와이프**: `touchstart`에서 자동재생을 멈추고 시작 X좌표를 기록, `touchmove`에서 이동한 픽셀을 컨테이너 폭 대비 %로 환산해 `translateX`에 실시간 반영(즉, 손가락을 따라 카드가 함께 움직임), `touchend`에서 이동량이 컨테이너 폭의 15~20% 이상이면 다음/이전 슬라이드로 확정하고 미만이면 원위치로 스냅
   - 점 인디케이터는 카드 개수만큼 버튼을 동적 생성하고 클릭 시 해당 인덱스로 이동 + 타이머 재시작
   - `matchMedia("(max-width: 640px)")`의 `change` 이벤트로 캐러셀 활성/비활성을 전환(모바일 진입 시 양쪽 클론 추가·타이머 시작, 데스크톱 복귀 시 클론 제거·타이머 정지·인라인 스타일 초기화)
   - `visibilitychange`로 탭이 백그라운드일 때 자동재생 일시정지 — 반드시 포함할 필요는 없지만 배터리/사용성 측면에서 권장
4. 카드 수가 2장 미만이면 캐러셀을 구성할 필요가 없으므로 함수 초입에서 `if (total < 2) return;`으로 조기 종료합니다.

## 13. SEO 기본 파일 세팅 (신규 프로젝트마다 반복 필요)

새 프로젝트를 정식 도메인으로 공개할 때 아래 3가지를 함께 세팅합니다.

1. `robots.txt`(사이트 루트) — `Allow: /`와 `Sitemap: https://{정식도메인}/sitemap.xml` 명시
2. `sitemap.xml`(사이트 루트) — 최소 홈 URL 1개(`<loc>`, `<lastmod>`, `<changefreq>`, `<priority>`)만 있어도 등록 가능, 페이지가 늘면 `<url>` 블록 추가
3. 네이버 서치어드바이저 소유확인 — 네이버가 발급하는 `navera{고유값}.html` 파일을 다운로드해 사이트 루트에 그대로 배포. **이 파일은 이후에도 계속 루트에 남아있어야 소유확인이 유지되므로 정리/청소 작업 시 실수로 삭제하지 않도록 주의**
4. 구글 서치콘솔 등록 시 속성(property)은 정식 도메인 표기(`www` 포함 여부)를 실제 `sitemap.xml`/`robots.txt`에 쓴 도메인과 정확히 일치시킬 것 — 다르면 "사이트맵 주소가 잘못됨" 에러 발생

## 14. 링크 공유 미리보기(OG) 이미지 크롭 방지 기법

카카오톡 등 메신저의 압축형 미리보기 카드는 `og:image`를 정사각형에 가깝게 크롭해서 보여주는 경우가 많아, 와이드 배너형 로고를 그대로 쓰면 좌우가 잘려 텍스트가 잘리는 문제가 생길 수 있습니다.

- 기존 헤더/푸터용 와이드 로고(`logo.png`)는 그대로 두고, `og:image`/`twitter:image` 전용으로 **정사각형(예: 900x900) 캔버스에 불투명 배경 + 로고를 여백 넉넉히 중앙 배치**한 별도 이미지(`og-logo.png`)를 만들어 그 태그에만 사용
- 이렇게 하면 플랫폼이 정사각형으로 자르든 와이드 비율(1.91:1)로 자르든 로고 전체가 항상 보임
- ffmpeg로 만들 때는 `pad` 필터 대신 `color=white:s=900x900` lavfi 소스를 배경으로 깔고 `overlay`로 합성해야 투명 배경이 검은색으로 뭉개지는 문제를 피할 수 있음

## 15. 새 프로젝트 착수 시 참고 순서

1. `설계서.md`를 읽고 현재 구조를 이해
2. 본 `지침.md`의 "2. 새 프로젝트 시작 체크리스트" 순서대로 진행
3. 데이터량(팀원 수, 지점 수, 영상 수)이 크게 다르면 "1-3. 데이터-마크업 분리"부터 먼저 리팩터링한 뒤 콘텐츠 입력
4. 완료 후 반응형 3단계 + 애니메이션 + 지도 점선 연결을 실제 브라우저에서 확인
5. "11. 배포 환경 구성" 절차대로 GitHub 저장소 생성 + Vercel 연결까지 초기 단계에 완료
6. 정식 도메인 연결 및 공개 시점에 "13. SEO 기본 파일 세팅"(robots.txt/sitemap.xml/네이버 소유확인) 진행
7. 카카오톡 등에 링크 공유 예정이면 "14. 링크 공유 미리보기 이미지 크롭 방지 기법"으로 `og:image` 전용 이미지 준비
