# 클린팩토리 홈페이지 프로젝트

## 프로젝트 개요

에어컨 청소 서비스 **클린팩토리**의 공식 홈페이지.
남양주를 거점으로 구리·하남·의정부·서울 동북부 지역에 출장 서비스를 제공한다.

- **주 고객층**: 40~60대 주택·아파트 거주자 (여성 비중 높음)
- **사이트의 목적**: 정보 전달이 아니라 **문의 전환**. 전화 또는 카카오톡 상담으로 이어지는 것이 유일한 성공 지표다.
- **핵심 설득 논리**: ① 눈에 보이지 않던 오염을 보여준다 ② 낯선 기사를 집에 들이는 불안을 없앤다

## 기술 스택

- 순수 HTML / CSS / JavaScript (빌드 도구 없음)
- 백엔드·DB·관리자 페이지 없음
- 배포: GitHub push 트리거형 Vercel 자동 배포

---

## 디자인 시스템 (최우선 규칙)

이 프로젝트는 두 개의 디자인 문서를 **역할을 나누어** 사용한다.

- `DESIGN-apple.md` — **골격**. 레이아웃, 섹션 리듬, 타이포 위계, 여백
- `DESIGN-airbnb.md` — **부품**. 카드, 폼, 후기, 프로필 등 사람이 등장하는 컴포넌트

### 절대 규칙

1. **색상·간격·타이포는 반드시 두 문서의 토큰을 참조한다. hex 값을 코드에 직접 쓰지 않는다.**
   모든 토큰은 `:root`의 CSS 변수로 정의하고, 그 변수만 사용한다.

2. **강조색은 파랑 단 하나.**
   - 기본: `#0066cc` (Apple Action Blue)
   - 포커스: `#0071e3`
   - 어두운 타일 위: `#2997ff`
   - **에어비앤비의 Rausch(`#ff385c`)는 이 프로젝트에서 사용하지 않는다.** 에어비앤비 컴포넌트를 차용할 때 강조색 자리는 전부 파랑으로 치환한다.
   - 두 번째 강조색을 도입하지 않는다. 모든 "누를 수 있는 것"은 파랑이다.

3. **폰트는 Pretendard를 사용한다.**
   SF Pro와 Airbnb Cereal은 한글 글꼴이 없으므로 그대로 쓰면 자간·무게 체계가 깨진다.
   Pretendard로 대체하되, 두 문서가 규정한 **크기·무게·자간·행간 값은 그대로 따른다.**
   - 무게 사다리: 300 / 400 / 500 / 600 / 700
   - 제목은 음수 자간(-0.28 ~ -0.44px)을 유지해 애플 특유의 조밀한 리듬을 낸다.

4. **본문은 16px / 400 / 행간 1.5.**
   애플 문서의 17px 대신 에어비앤비의 16px를 채택한다. 한글 가독성 기준이다.
   행간을 1.5 아래로 좁히지 않는다.

5. **모서리 문법을 섞지 않는다.**
   - 풀블리드 섹션 타일: 각지게(radius 0). 색 전환 자체가 구분선이다.
   - 섹션 안의 개별 카드: 둥글게(14px, 에어비앤비 방식)
   - 버튼·검색·칩: 완전한 알약형
   - 이 셋 사이의 중간값을 임의로 만들지 않는다.

6. **그림자는 사진에만.**
   버튼, 카드, 텍스트에 그림자를 넣지 않는다. 유일한 예외는 표면 위에 놓인 제품·기기 사진이다.

7. **장식용 그라디언트를 쓰지 않는다.** 분위기는 사진이 만든다.

8. 버튼의 눌림 상태는 `transform: scale(0.95)`로 통일한다.

### 섹션 리듬

밝은 타일(`#ffffff`) → 어두운 타일(`#272729`) → 밝은 타일을 교대로 쌓는다.
두 밝은 타일이 연속될 때는 사이에 `#f5f5f7`(연회색)을 넣어 끊는다.
섹션 상하 여백은 데스크톱 80px, 모바일 48px.

---

## 콘텐츠 원칙

### 사진

이 디자인은 사진이 약하면 성립하지 않는다. UI를 비운 만큼 사진이 화면을 책임진다.

- 시공 전후 사진은 `before-01.jpg` / `after-01.jpg` 처럼 짝을 이루는 이름을 쓴다
- 모든 이미지에 의미 있는 `alt` 속성을 채운다 (검색 노출과 접근성 양쪽에 필요)
- `loading="lazy"`를 기본으로 하되, 첫 화면 사진은 즉시 로드한다
- 실제 시공 사진만 사용한다. 스톡 이미지를 쓰지 않는다

### 문구

- 과장 표현("100% 제거", "업계 최고")을 쓰지 않는다. 신뢰가 이 사이트의 자산이다
- 전문 용어보다 고객이 실제로 쓰는 말을 쓴다 ("열교환기" 대신 "에어컨 속 냉각판")
- 가격은 숨기지 않는다. 불투명한 가격은 문의를 막는다

---

## 페이지 구조

### 메인 페이지 (`/`)

한 페이지 안에 섹션으로 쌓는다. 상단 메뉴는 페이지 이동이 아니라 해당 섹션으로 스크롤한다.

1. 히어로 — 대표 전후 사진 + 한 줄 메시지 + 문의 CTA
2. 이런 증상이라면 — 청소가 필요한 신호 (곰팡이 냄새, 냉방 약화 등)
3. 시공 과정 — 완전분해 청소 단계를 사진으로
4. 시공 갤러리 — 대표 사례 6~8건, 전후 비교
5. 가격 안내 — 기종별 요금표
6. 기사 소개 — 프로필, 경력, 보유 장비
7. 후기
8. 지역 안내 — 각 지역 페이지로 연결

### 지역 페이지

| 경로 | 대상 |
|---|---|
| `/namyangju` | 남양주 |
| `/guri` | 구리 |
| `/hanam` | 하남 |
| `/uijeongbu` | 의정부 |

**지역 페이지는 메인 내용을 복사해 지역명만 바꾸는 방식을 절대 쓰지 않는다.**
검색엔진이 중복 문서로 판단해 전부 색인에서 밀려난다.
각 페이지는 그 지역의 실제 시공 사례, 사진, 출장 안내로 채운다.

---

## 전환 요소 (두 디자인 문서에 없는 자체 설계)

### 하단 고정 문의 바

애플 문서의 `floating-sticky-bar`를 변형한다.
스크롤 중 화면 하단에 계속 떠 있으며, 왼쪽은 전화 버튼, 오른쪽은 카카오톡 상담 버튼.
모바일에서는 화면 폭 전체를 쓰고, 터치 영역은 최소 44×44px를 지킨다.

### 전후 비교 슬라이더

가운데 손잡이를 좌우로 끌면 오염 상태와 청소 후가 겹쳐 보이는 UI.
**이 사이트에서 전환율에 가장 크게 기여할 요소이므로 완성도를 우선한다.**
모바일 터치 드래그를 반드시 지원한다.

---

## SEO 요건

이룸 프로젝트에서 뒤늦게 붙였던 항목들을 이번에는 처음부터 넣는다.

- 페이지마다 고유한 `title`과 `meta description` (지역 페이지는 지역명 포함)
- `h1`은 페이지당 하나. `h1 → h2 → h3` 순서를 건너뛰지 않는다
- `LocalBusiness` 구조화 데이터(JSON-LD) — 상호, 주소, 전화번호, 영업시간, 서비스 지역
- `sitemap.xml`, `robots.txt`, `favicon.ico`, `canonical`, Open Graph 태그
- 지역+서비스 조합 키워드를 노린다 (예: "남양주 에어컨청소", "구리 벽걸이에어컨 분해청소")
- 히어로 영상·이미지 용량을 관리한다. 로딩 속도가 곧 순위다

---

## 반응형

주요 분기점: 1440px(콘텐츠 고정) / 1068px / 833px / 640px / 480px

- 모바일 우선으로 확인한다. 이 업종은 모바일 유입이 대다수다
- 히어로 제목: 56px → 40px(1068px) → 34px(640px) → 28px(480px)
- 갤러리 그리드: 4열 → 3열(1068px) → 2열(833px) → 1열(640px)
- 모든 터치 대상은 최소 44×44px

---

## 작업 방식

- 한 번에 한 섹션씩 완성하고 확인한다. 전체를 한꺼번에 만들지 않는다
- 새 컴포넌트를 만들기 전에 두 디자인 문서에 해당 컴포넌트가 있는지 먼저 확인한다
- 임의로 색·크기·간격을 만들어내지 않는다. 필요하면 먼저 물어본다
- 커밋 메시지는 한글로, 무엇을 왜 바꿨는지 적는다
